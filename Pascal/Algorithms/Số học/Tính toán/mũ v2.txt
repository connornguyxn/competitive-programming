function mu(a,b:int64):int64;
var k,h:int64;
begin
if b = 1 then exit(a mod 10000);
k:=b div 2;
h:=a*a mod 10000;
if b mod 2 = 0 then exit(mu(h,k) mod 10000)
    else 
        exit((mu(h,k) mod 10000) * a mod 10000);
end;
