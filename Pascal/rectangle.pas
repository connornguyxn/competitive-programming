var a,i:longint;
///////////////////////////////////////////////////////////
begin
assign(input,'rectangle.inp');reset(input);
assign(output,'rectangle.out');rewrite(output);
readln(a);
for i:=trunc(sqrt(a)) downto 1 do
    begin
    if sqr(sqrt())

    end;
end.
