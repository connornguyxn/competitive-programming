var qs,q,n,i:longint;
{=====================================}
begin
readln(qs);
for q:=1 to qs do
    begin
    readln(n);
    if (n < 3) then
        begin
        writeln(-1);
        continue;
        end;
    for i:=2 to sqrt(n) do
        begin
        
        end;
    end;
end.