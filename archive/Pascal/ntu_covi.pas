var x1,x2,y1,y2,x,y:longint;
///////////////////////////////////////
begin
readln(x1,y1,x2,y2);
x := abs(x1 - x2);
y := abs(y1 - y2);
if (x = 0) then
    begin
    writeln(value)
    end
else if (y = 0) then
    begin
    
    end;
end.